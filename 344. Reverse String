class Solution {
    public void reverseString(char[] s) {
        int first;
        int last = s.length-1;

        for(first=0; first<last; first++){
            char temp=s[last];
            s[last]=s[first];
            s[first]=temp;
            last--;
        }
    }
}
